1. Check rollout status:
   kubectl -n {{ .Release.Namespace }} rollout status deployment/{{ include "sdu-uct-analiz.fullname" . }}
{{- if .Values.clickhouse.enabled }}
   kubectl -n {{ .Release.Namespace }} rollout status deployment/{{ include "sdu-uct-analiz.fullname" . }}-clickhouse
{{- end }}

2. Open the dashboard:
{{- if .Values.ingress.enabled }}
{{- range .Values.ingress.hosts }}
   https://{{ .host }}
{{- end }}
{{- else }}
   kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "sdu-uct-analiz.fullname" . }} 8080:{{ .Values.app.service.port }}
   http://127.0.0.1:8080
{{- end }}

The bundled ClickHouse image already contains 311 settlements, 2102 appeals and 13 district rows.
No migration or initialization Job is created by this chart.
